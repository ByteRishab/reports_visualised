The project aims to generate an XML tree that can resemble a report structure on an SSRS reporting platform

# we use XML and OOP orientation to create the structure and will eventually try to pass Inputs through Excel file system
